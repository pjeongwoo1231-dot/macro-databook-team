# macro-databook runtime dependencies
#
# ASCII only on purpose: pip reads this file with the locale encoding
# (cp949 on Korean Windows) unless it has a BOM, so non-ASCII comments
# crash the install with UnicodeDecodeError. Keep notes in Korean out of
# this file - see README.md.
#
# Stdlib-only parts are not listed: the collector runs on urllib/json/re/xml,
# and the news digest needs no third-party package.

PyYAML>=6.0        # indicators.yaml parsing - nothing works without it
feedparser>=6.0    # RSS ingestion (intel / some news paths)
numpy>=1.24        # derived indicators, curve fitting
xlrd>=2.0          # EIA crude .xls reader (FRED crude series retired)
