# 논문 분석 — 팀원용 (옵시디언 싱크 환경)

각자 **다른 논문**을 맡아 읽고, 볼트에 노트로 남긴다. 그 노트가 제텔·지표와 링크로 만나서
나중에 시황·리포트의 근거가 된다.

이 문서는 **싱크로 라이브 볼트를 함께 쓰는 팀원**용이다.
배포본 ZIP만 받아 읽는 사람은 [`SETUP.md`](SETUP.md)를 보면 된다 — 둘은 **같이 쓰지 않는다**(§0).

---

## 0. 먼저 — 배포본과 싱크는 상호배타다

`bootstrap.py` 는 볼트를 **통째로 지우고 다시 푼다**(`shutil.rmtree`). 배포본은 읽기용
사본이라 그게 맞는 동작이다. 그런데 싱크가 붙은 라이브 볼트에서 이게 돌면
**삭제가 팀 전원에게 전파된다.** 한 사람의 실수가 전원의 자료를 지우는 유일한 경로다.

그래서 `bootstrap.py` 에 가드를 넣었다. 싱크 볼트로 보이면 아무것도 지우지 않고 멈춘다.

```
[중단] ~/MacroVault 는 라이브(싱크) 볼트로 보입니다 — **아무것도 지우지 않았습니다.**
       발견한 흔적:
         - .obsidian/sync.json  (Obsidian Sync 연결)
```

**싱크를 쓰는 팀원은 `bootstrap.py` 를 돌릴 일이 없다.** 자료는 싱크로 이미 최신이다.

---

## 1. 논문을 맡는다 (claim)

**노트를 먼저 만들지 않는다.** 파일명을 선점하는 게 먼저다 —
둘이 같은 논문을 잡으면 싱크가 둘 중 하나를 말없이 덮는다.

```bash
python tools/paper_assign.py claim \
    --paper "A Contribution to the Theory of Economic Growth" \
    --year 1956 --author "Solow" --who 홍길동
```

```
배정 완료 — 홍길동
  노트명: 1956 A Contribution to the Theory of Economic Growth (Solow).md   ← 선점됨
  마감  : 2026-09-18
```

이미 누가 맡았으면 그 자리에서 막힌다.

```
[중단] 이미 배정된 논문입니다 — 담당 홍길동 (2026-09-11)
```

### 왜 표가 아니라 파일 하나씩인가

배정 현황을 표 하나로 두면 **그 표가 유일한 동시편집 지점**이 된다. 12명이 같은 줄
근처를 고치면 싱크 충돌이 거기서 난다. 그래서 청구는 `03_MOC/논문배정/<노트명>.md` 로
**1건 1파일**이다. 서로 다른 논문을 맡는 한 두 사람이 같은 파일을 건드릴 일이 없다.

`03_MOC/논문 배정 큐.md` 는 그 청구들을 모아 **자동 생성**한다 — 손으로 고치면 덮어쓴다.

---

## 2. 노트를 연다 (start)

```bash
python tools/paper_assign.py start "1956 A Contribution to the Theory of Economic Growth (Solow)"
```

`_System/Templates/T_Paper.md` 를 바탕으로 `02_Papers/` 에 노트가 생기고,
제목·연도·저자·작성일만 채워진 채 나머지는 `{{...}}` 로 남는다. 상태는 `drafting` 이 된다.

---

## 3. 쓴다 — 규칙은 둘뿐

**① 확인한 것만 적는다.**
저널·권호·DOI·공저자는 **실제로 조회한 것만** 채운다. 확인 못 한 칸은
지어내지 말고 `미확인` 으로 비워 둔다. 이건 취향이 아니라 사고 이력이 있는 규칙이다 —
2인/2023 으로 적어둔 논문이 실제로는 3인/2024 였던 적이 있다.

**② 인과 사슬을 쓴다.**
"~에 관해 다룬다"는 사슬이 아니다. 무엇이 무엇을 움직이는지 방향을 만든다.
여기서 만든 `[[링크]]` 가 지표 노트의 Backlinks 에 쌓이고, 그게 이 볼트의 자산이다.

```
[[지정학적 리스크]] ↑ → [[글로벌 공급망]] 병목 → [[WTI (국제유가)]] ↑ → [[CPI (소비자물가지수)]] 압력
```

---

## 4. 상태를 옮긴다

```bash
python tools/paper_assign.py set "<노트명>" review    # 남에게 봐달라고 할 때
python tools/paper_assign.py set "<노트명>" done      # 끝
python tools/paper_assign.py status                   # 전체 현황
```

| 상태 | 뜻 |
|---|---|
| `claimed` | 맡았다. 아직 노트 없음 |
| `drafting` | 노트를 만들었고 쓰는 중 |
| `review` | 초고 완료, 검토 요청 |
| `done` | 끝 |

마감이 지났는데 `done` 이 아니면 현황과 큐 노트에 `⚠` 가 붙는다.

---

## 5. 하지 말 것

- **claim 없이 `02_Papers/` 에 노트를 만들지 않는다** — 파일명 충돌이 조용히 덮는다.
  이 볼트에서 실제로 59건이 났었다.
- **`03_MOC/논문 배정 큐.md` 를 손으로 고치지 않는다** — 자동 생성물이다.
- **`04_DataBook/` 을 건드리지 않는다** — 수집기가 매일 덮어쓰는 영역이다.
  숫자가 틀렸으면 수집 담당에게 말한다.
- **남의 노트를 고치지 않는다** — 이견은 그 노트에 코멘트로 남기거나 제텔로 분리한다.

---

## 6. 수집기와 겹치지 않나

겹치지 않는다. 경로가 다르다.

```
수집기(run)  →  04_DataBook/              ← publish.py 가드가 지키는 범위
팀원(논문)   →  02_Papers/ · 07_Zettel/    ← 수집기가 건드리지 않음
```

수집은 **한 사람만** 돌린다. 여러 명이 같은 볼트에 발행하면 서로 덮는다 —
2026-09-01 에 키 없는 환경의 런이 팀 전원 Data Book 을 311개에서 168개로 바꿔놓은 적이 있다.
분석은 그 위험이 없다. 경로가 분리돼 있기 때문이다.
