# 팀원용 설치 가이드

매크로 Data Book 자동수집기를 **내 컴퓨터에서 돌려서 Obsidian에 넣기**까지 5분.
(코드를 고치거나 지표를 추가하려면 `AGENTS.md`를 보세요. 이 문서는 "그냥 쓰기" 용입니다.)

---

## 0. 이게 뭘 해주나

`python -m databook run` 한 번에 **330개 지표를 공개 API에서 받아서** 팀별 마크다운 4개 +
스냅샷 JSON을 만듭니다. Obsidian 볼트 경로를 지정하면 거기에도 바로 씁니다.

**출력물에는 해석 문구가 없습니다** — 숫자·기준일·출처 링크만. 판단은 사람 몫이라는 게 이 도구의 규칙입니다.

### 0-1. 먼저 내가 어느 쪽인지 정하세요 — 대부분은 키가 필요 없습니다

| | 읽기만 하는 사람 (대부분) | 수집 담당 (1명) |
|---|---|---|
| 하는 일 | 옵시디언 싱크로 Data Book을 받아 읽는다 | 매일 `run`을 돌려 볼트에 발행한다 |
| 설치 | **없음** (옵시디언만) | 이 저장소 + Python |
| API 키 | **하나도 필요 없음** | 전부 필요 |
| 볼 것 | §4-1 (`.json` 토글) | §1 ~ §4 전부 |

**수집은 한 사람만 돌리는 게 맞습니다.** 여러 명이 같은 볼트에 발행하면 서로 덮어씁니다 —
2026-09-01에 키 없는 환경에서 돌린 런이 팀 전원의 Data Book을 311개에서 168개로
바꿔 놓은 게 정확히 그 사고입니다(§4-2).

읽기만 하는 사람도 `show`·`diff`를 쓰고 싶으면 저장소를 받되 **키는 여전히 필요 없습니다**
(볼트에 온 스냅샷을 읽습니다 — §4-1의 토글만 켜세요).

키가 필요한 건 **직접 수집을 돌릴 때뿐**입니다. 그때도:
- **328개 중 184개는 키가 아예 필요 없습니다.** FRED 하나만 받으면 289개가 돕니다
- 키는 전부 **무료·즉시 발급**입니다. 남의 키를 받아 쓰지 마세요 —
  레이트리밋이 키 단위라(BLS 일 500건, 공공데이터포털은 서비스별 할당) 같이 쓰면 같이 막힙니다
- **토스증권만은 공유가 물리적으로 불가능**합니다(허용 IP가 사람마다 다름 — §2-1)

---

## 1. 설치 (3분)

```bash
# Python 3.11 이상 필요
python --version

git clone <저장소 주소>
cd macro-databook
pip install -r requirements.txt
```

필수 의존성은 `PyYAML` 하나입니다. 나머지는 선택이고, 없으면 해당 지표만 실패합니다.

---

## 2. 키 설정 (2분) — 마법사가 다 알려줍니다

```bash
python -m databook setup
```

실행하면 **없는 키 목록 + 발급 링크 + 그 키가 살리는 지표 수**를 한꺼번에 보여줍니다.
링크를 전부 열어 키를 받아온 뒤 한 번에 붙여넣으면 됩니다. 이런 식입니다:

```
전체 지표 328개 중 184개는 키가 전혀 필요 없습니다
현재 설정된 키로 도는 지표: 약 184개

── 아직 없는 키 — 아래 링크를 한꺼번에 열어 발급받은 뒤 붙여넣으세요
   [필수] 미국·글로벌 지표 (FRED)
            105개   https://fred.stlouisfed.org/docs/api/api_key.html
   [필수] 한국은행 경제통계 (ECOS)
             14개   https://ecos.bok.or.kr/api/
   ...
```

입력이 끝나면 **키가 진짜 살아있는지 실제로 호출해 확인**합니다
(FRED·ECOS·e-Stat). 오타를 붙여넣고 수집이 다 끝난 뒤에야 아는 일이 없습니다.

```
── 키 확인 중…
   ✓ FRED_API_KEY: 정상
   ✗ ECOS_API_KEY: 인증 실패 — 키를 다시 확인하세요
```

**키가 하나도 없어도 실행은 됩니다.** 관세청·일본은행·중국인민은행·SEC·DBnomics 등
**184개(56%)는 키가 아예 필요 없습니다.** 하나만 받으라면 **FRED**입니다 — 이거 하나로 289개가 돕니다.

> 손으로 채우고 싶으면 `cp .env.example .env` 후 직접 편집해도 됩니다.
> 어떤 키가 무엇을 살리는지 `.env.example`에 전부 적혀 있습니다.

> ⚠ **`.env`는 절대 공유하지 마세요.** `.gitignore`에 들어 있어 커밋되지 않지만,
> 카톡·메일로 파일을 통째로 보내면 그대로 새어 나갑니다.
> 특히 `TOSSINVEST_*`는 **개인 증권계좌에 연결된 자격증명**이라 남에게 주고받지 마세요.

### 2-1. 토스증권만은 각자 발급합니다 (국내 수급 10개)

받아 쓸 수가 없습니다. 토스 앱은 **허용 IP에 묶여** 있어서, 남의 Client ID/Secret을
그대로 넣으면 **키가 맞아도 403**입니다. (게다가 토큰이 계정당 동시에 하나뿐이라,
한 키를 둘이 쓰면 서로 401을 만듭니다.) 그래서 마법사가 이 항목만 따로 세워
**지금 이 PC의 공인 IP를 화면에 띄워 줍니다** — 그 값을 그대로 등록하면 됩니다.

```
── 본인 것만 통하는 키 — 남에게 받아 쓸 수 없습니다
   [선택] 토스증권 Open API — Client ID
             10개   https://developers.tossinvest.com

   토스 앱은 **허용 IP에 묶여** 있습니다. 남의 Client ID/Secret을 그대로 넣으면
   키가 맞아도 403이 납니다. 각자 앱을 만들고 자기 IP를 등록하세요 (무료·즉시).
     1) https://developers.tossinvest.com 로그인 → 앱 생성
     2) 허용 IP에 지금 이 PC의 공인 IP를 등록:  118.235.xx.xx
     3) 발급된 Client ID / Secret을 아래에 붙여넣기
```

넣으면 **토큰 발급 + 실제 조회까지 한 번 해 보고** 결과를 말해 줍니다.
토큰만 확인하면 안 되는 이유는 IP 검사가 **조회 단계**에서 걸리기 때문입니다.

```
   ✓ TOSSINVEST_CLIENT_ID: 정상 — 토큰 발급 + 실호출까지 확인
   ✗ TOSSINVEST_CLIENT_ID: 허용 IP 미등록 — 키는 맞고 IP가 안 맞습니다 · 지금 이 PC의 공인 IP: 118.235.xx.xx
```

⚠ **공유기·회사망·핫스팟은 IP가 바뀝니다.** 집에서 되던 게 학교에서 403이 나면
키를 다시 발급받지 말고 **허용 IP만 갱신**하세요. 수집 중 403이 나면 그때도
현재 공인 IP를 함께 찍어 줍니다.

⚠ 안 넣어도 됩니다 — **국내 수급 10개만** 비고 나머지 318개는 정상입니다.

---

## 3. 실행

```bash
python -m databook run          # 전체 수집
python -m databook run --dry-run    # 네트워크 없이 설정만 검증
python -m databook run --no-news    # 뉴스 빼고 수집
python -m databook.news         # 뉴스만 따로 (run에 이미 포함)

python -m databook weekly    # ★ 세션 준비 한 방 — 아래 5개를 한 덩어리로
                            #   ① 수집 상태 ② 기준 시점(as-of) 전체 상태
                            #   ③ 지난 한 주 변경분 ④ 장기 시계열 위치
                            #   ⑤ 새 기사 · 손댈 자리

```

## 3-1. 조회 — 통째로 읽지 마세요

산출물이 큽니다. 뉴스 다이제스트는 **약 668,000토큰**이라 AI 컨텍스트에 아예 안 들어갑니다.
필요한 것만 뽑는 명령이 있습니다.

```bash
python -m databook show 중국 PPI          # 지표 하나만        20,837 → 225 토큰
python -m databook diff                   # 어제 대비 변경분만 255,687 → 6,880
python -m databook diff --since 2026-08-25 --until 2026-09-01   # 구간 고정
python -m databook news --q 중국 --new    # 어제 없던 새 기사   668,037 → 1,059
python -m databook news --team 4팀 --limit 50
```

```bash
python -m databook todo                 # 에이전트가 채울 자리 (약 1,400 토큰)
python -m databook todo --json          # 파싱하기 좋게
```

`todo`는 **AI에게 넘길 작업 목록**입니다. 자동 경로가 없는 `manual` 슬롯과,
원본이 갱신을 멈춘 `STALE` 계열을 **출처 URL과 함께** 뽑아줍니다.
에이전트에게 그대로 주면 원문을 열어 값을 채웁니다.

> 트리거 판정은 여기 없습니다 — `derived.py`가 이미 코드로 합니다(17종, 임계값 포함, 토큰 0).

⚠ `news`는 **검색이지 요약이 아닙니다.** 무엇을 볼지는 사람이 정합니다 —
AI가 골라주면 그 자체가 편향이라, 수집 단계부터 취사선택을 금지하고 있습니다.
```

끝나면 이렇게 나옵니다:

```
완료: 성공 322 / 실패 2 / 수동 3 / 전체 328
  → .../output/Macro/1_Growth/DataBook_1_2026-08-28.md
  → .../output/snapshot_2026-08-28.json
```

**실패가 몇 개 있는 게 정상입니다.** 대부분 "키 없음"이거나 원본 사이트의 일시적 차단입니다.
지표 하나가 실패해도 전체 실행은 끝까지 갑니다.

---

## 4. Obsidian에 연결

`.env`에 볼트 경로를 넣으면 실행할 때마다 볼트에도 씁니다.

```
OBSIDIAN_VAULT_PATH=C:\Users\<이름>\MacroVault
DATABOOK_OUTPUT_DIR=C:\Users\<이름>\macro-data\output
```

`DATABOOK_OUTPUT_DIR`은 **꼭 저장소 바깥으로** 잡으세요.
저장소가 OneDrive·Dropbox 안에 있으면 매일 바뀌는 산출물이 계속 동기화됩니다.

볼트에 `01_Indicators/` 폴더가 있으면 `04_DataBook/DataBook 지표 소환.md` 허브를 만들어
지표 노드를 `[[...]]`로 부릅니다. 볼트가 없으면 이 단계는 자동으로 건너뜁니다.

### 4-1. 배포본(ZIP)을 받아 쓰는 사람 — 볼트 경로만 적으면 끝입니다

> ⚠ **2026-09-02부로 배포가 바뀌었습니다.** 옵시디언 싱크 → **주 1회 ZIP(구글드라이브)**.
> 예전 안내였던 "설정 → 동기화 → '기타 모든 파일 형식' 켜기"는 **이제 하지 마세요** —
> 켤 싱크가 없습니다. 그 안내를 따라가다 막힌 것이면 잘못은 안내 쪽입니다.

받는 법:

1. 구글드라이브에서 `MacroVault_<기준일>.zip`을 받아 압축을 풉니다
2. Obsidian → **다른 폴더를 볼트로 열기** → 압축 푼 폴더
3. `show`·`diff`·`weekly`를 쓰려면 이 저장소의 `.env`에 한 줄만 적습니다

```
OBSIDIAN_VAULT_PATH=C:\Users\<이름>\MacroVault_2026-09-03
```

**API 키는 한 개도 필요 없습니다.** 배포본에 스냅샷(`04_DataBook/snapshots/*.json`)과
장기 시계열(`04_DataBook/history/`, 180계열)이 들어 있어서 그대로 읽습니다.

```bash
python -m databook show 중국 PPI     # 배포본 스냅샷에서 읽습니다
python -m databook weekly            # 배포본 기준일을 [수집 상태]에 찍어 줍니다
```

**ZIP 파일명의 날짜가 인용 기준일입니다.** 그 주의 인용은 이 하나로 통일합니다 —
각자 수집기를 돌려 나온 값은 기준일이 달라 서로 다른 숫자를 말하게 됩니다.

배포본에 **없는 것**: 원문 PDF(구글드라이브 별도 폴더) · `daily.log`(수집자 PC의 로그) ·
`history/toss/` 종목 패널(593MB).

#### 배포본을 만드는 쪽 (수집 담당자만)

```bash
python <볼트>/_System/package_vault.py            # 볼트 옆 MacroVault_dist/ 에 만듭니다
python <볼트>/_System/package_vault.py --dry-run  # 넣을 목록만 확인
```

기준일은 **볼트 최신 스냅샷 날짜**로 박힙니다(빌드한 날이 아니라). 결과는 약 16MB입니다.

### 4-2. 망가진 런은 볼트에 안 들어갑니다

**2026-09-01에 실제로 사고가 났습니다.** `.env`가 없는 폴더에서 `run`이 돌아
키가 필요한 지표가 전부 "수집 실패 — FRED_API_KEY 없음"이 된 채로 볼트에 쓰였고,
싱크를 타고 팀원 전원의 Data Book이 **311개 → 168개**로 바뀌었습니다.

이제 막습니다. 발행 직전에 **볼트에 이미 있는 최근 5회 인덱스 노트**와 비교해
수집 수가 **20% 넘게 줄었으면 볼트에 쓰지 않습니다.**

```
⚠ 볼트 발행 중단: C:\Users\<이름>\MacroVault\04_DataBook
  최근 발행 최대 325개(2026-08-30) → 이번 168개. 20% 넘게 줄었다
  원인 후보: 필수 키가 비어 있다 — FRED_API_KEY, ECOS_API_KEY
           이 저장소에 .env가 없거나 다른 폴더에서 돌린 것은 아닌지 확인
  → 로컬 output/ 에는 그대로 썼다. 확인 후 정말 발행하려면 `python -m databook run --force-publish`
```

로컬 `output/`에는 그대로 씁니다 — 무엇이 깨졌는지 봐야 하니까요.
지표를 대량으로 정리해서 **정말 줄어든 것**이면 `--force-publish`로 한 번 올리면
그 값이 새 기준선이 됩니다.

---

## 5. 자주 나오는 실패와 뜻

| 표시 | 뜻 | 할 일 |
|---|---|---|
| `키 없음 (.env 확인)` | 그 소스 키가 비어 있음 | `.env.example` 보고 발급 |
| `HTTP Error 403` | 원본이 봇 차단 | 대개 일시적. 반복되면 보고 |
| `STALE — 최신 2026-02 (179일 전)` | 원본이 갱신을 멈춤 | **값은 맞지만 낡음.** 기준일을 꼭 같이 인용 |
| `수동 입력` | 자동 경로가 없는 지표 | 사람이 채우는 자리 (사유는 note에) |
| `IP address not allowed` (토스) | 앱에 등록한 허용 IP 밖에서 호출 | 키 재발급 말고 **허용 IP만 갱신** (§2-1) |
| `볼트 발행 중단` | 이번 런이 최근 발행보다 크게 나쁨 | 원인 고치고 재실행 (§4-2) |

`STALE`은 특히 중요합니다. 중국 NBS 계열은 현재 6개월 지연이라
**"최신 수치"라고 말하면 틀립니다.** 항상 기준일을 함께 읽으세요.

---

## 6. 지표를 추가하고 싶다면

`indicators.yaml` 하나만 고치면 됩니다 — 기존 소스를 쓰는 지표라면 **코드 수정이 필요 없습니다.**
새 소스가 필요하면 `AGENTS.md`의 개발 지침을 보세요.

**절대 규칙 하나**: 계열 코드·통계표 ID를 **추측으로 채우지 마세요.**
반드시 해당 API를 실제로 호출해 확인한 뒤 적고, 확인 못 하면 `manual`로 두고 사유를 남깁니다.
이 저장소의 함정 대부분이 "그럴듯하지만 틀린 코드"에서 나왔습니다.
