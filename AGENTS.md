# AGENTS.md — AI 하네스 작업 지침

이 저장소를 Claude Code·Codex 등 AI 터미널에서 열었다면, 목적을 먼저 판단한다:
**(A) 다운받아 그냥 돌리려는 사용자를 돕는가 → 아래 "온보딩 실행"**, (B) 코드를 고치거나 지표를 늘리려는가 → 그 아래 개발 지침.

## 🚀 온보딩 실행 (다운받은 사람을 돕는 경우)

사용자가 이 저장소를 클론/붙여넣고 "실행해줘" 류로 요청하면, 아래를 **순서대로 자동 수행**한다.
멈춰서 물어볼 지점은 딱 하나(=API 키)뿐이다.

1. **Python 확인**: `python --version` 이 3.11+ 인지 확인(아니면 3.11+ 설치 안내).
2. **의존성 설치**: `pip install -r requirements.txt` (필수는 PyYAML 하나, 나머지는 선택).
3. **키 설정**: `.env` 가 없거나 필수 키(FRED·ECOS·KOSIS·DATA_GO_KR)가 비어 있으면 —
   - 사용자에게 네 개의 **무료** 키 발급을 안내한다(발급처는 `README.md`·`python -m databook setup` 출력 참조).
   - 키를 받으면 대화형 마법사 `python -m databook setup` 을 사용하거나, 값을 받아 `.env`(형식은 `.env.example`)에 직접 기록한다.
   - **키는 절대 로그·출력·커밋에 남기지 않는다.** `.env` 는 이미 `.gitignore` 대상.
   - Obsidian에 바로 넣으려면 `OBSIDIAN_VAULT_PATH` 에 사용자 vault 경로를 받는다(선택).
4. **실행**: `python -m databook run` (자동 수집) → 이어서 `python -m databook.news` (뉴스, 키 불필요).
5. **결과 보고**: "성공 N / 실패 M / 수동 K / 전체" 요약과 산출물 경로(`output/` 또는 vault의 `Macro/`)를 알려준다.
   실패한 지표는 대부분 키 미설정 또는 무료 API 부재이니, 어떤 키를 넣으면 몇 개가 살아나는지만 짚어준다.

비대화형 환경(CI 등)이면 `python -m databook run --no-setup` 으로 마법사를 건너뛰고, 키는 환경변수/Secrets로 받는다.

---

## 프로젝트가 무엇인가
`SPEC.md`에 정의된 "매크로 Data Book 자동 최신화" 앱을 이 폴더에 구현한다.
지표 목록·API 매핑의 단일 소스는 `indicators.yaml`이다. 지표를 코드에 하드코딩하지 말 것.

## 구현 순서 (권장)
1. `method: api` + `source: fred` 지표 전체 (가장 많고 가장 안정적) → md/json 렌더까지 관통
2. FiscalData·TreasuryDirect·CoinGecko·DefiLlama·업비트·바이낸스 (키 불필요 소스)
3. ECOS·KOSIS·공공데이터포털 (한국 파트 — 아래 verify 규칙 준수)
4. 파생 지표 4종 (순유동성, 2s10s, 한미 금리차, 김치프리미엄)
5. `method: scrape` (FedWatch, MOVE, CBOE, KRX, KOFIA, Farside) — 실패해도 전체가 죽지 않게
6. `method: manual` — Data Book에 빈 슬롯 + 출처 링크만 생성

## 절대 규칙
- **코드를 추측으로 채우지 말 것**: `indicators.yaml`에 `verify`로 표시된 ECOS `stat_code`/`item_code`,
  KOSIS 표 코드, 공공데이터포털 endpoint는 반드시 해당 목록 조회 API를 실제 호출해 확인한 뒤 yaml을
  갱신하고 사용한다. 확인 못 하면 그 지표는 `manual`로 강등하고 사유를 yaml note에 남긴다.
- **해석 금지**: 출력 md에는 숫자·기준일·출처 링크만. "상승세", "긴축적" 같은 해석 문구를 생성하지 않는다.
- **실패 격리**: 지표 1개 실패 = 그 지표만 "수집 실패 (사유)" 표기. 전체 실행은 완주.
- API 키는 `.env`에서만 읽는다 (`.env.example` 참조). 키를 코드·로그·출력물에 남기지 않는다.
- 모든 수치에 관측 기준일(observation date)과 수집 시각(UTC)을 붙인다.

## 검증 방법
- `python -m databook run --dry-run`: 네트워크 없이 yaml 파싱·렌더 경로만 검증
- `python -m databook run --only fred`: 소스 단위 부분 실행 지원
- 완료 기준은 SPEC.md의 "수용 기준" 5개 항목. 구현 후 실제 실행으로 md/json 산출물을 열어 확인할 것.

## 스타일
- Python 3.11+, 타입힌트 사용, 외부 의존성 최소 (httpx 또는 requests, pyyaml; pandas는 선택)
- fetcher는 `fetchers/` 아래 소스당 1파일, 공통 인터페이스: `fetch(indicator: dict) -> Series | FetchError`

## Obsidian 지표 노드 소환 (2026-08-14 추가)

`OBSIDIAN_VAULT_PATH`가 설정돼 있고 볼트에 `01_Indicators/`가 있으면,
매 실행마다 **`04_DataBook/DataBook 지표 소환.md` 허브 하나**를 덮어쓴다.
이 허브가 지표 노드를 `[[...]]`로 부르고, 노드의 Backlinks에서
**최신 수치와 논문·제텔이 만난다.**

### 왜 허브 하나인가

일별 노트가 각자 지표 노드를 소환하면 1년 뒤 `[[통화정책]]` 백링크가
Data Book 파일 수백 개로 덮여 문헌 신호가 죽는다. 그래서
**소환은 허브만 하고 일별 노트는 허브로 연결만 한다** — 노드당 백링크 1개.

### 매칭 규칙

`databook/vaultlink.py`가 볼트의 `01_Indicators/*.md`에서 파일명(stem)과
`aliases:`를 읽어 토큰 사전을 만들고, 지표 이름에 그 토큰이 들어 있으면 잇는다.
**최장일치 우선**, 토큰 2자 이상. 볼트나 폴더가 없으면 빈 매핑을 돌려주고
출력은 아무것도 바뀌지 않는다 — 노드 이름을 코드에 넣지 않는 이유다.

수동 지정은 `indicators.yaml`의 선택 필드로 한다.

```yaml
  - name: 재고 사이클 (재고/판매 비율)
    vault_node: "-"        # 자동 매칭 끄기 (오매칭 차단)
  - name: 어떤 지표
    vault_node: 기준금리    # 이 노드로 강제
```

실제로 `재고 사이클 (재고/판매 비율)`이 별칭 `재고` 때문에 `[[원자재 재고]]`
(LME 구리 등 실물 재고)로 잘못 붙어서 `-`로 막았다. 오매칭을 발견하면 같은 방식으로 처리한다.

### 커버리지 (2026-08-13 스냅샷 기준)

지표 224개 중 **104개가 29개 노드**에 연결됐다. 나머지는 볼트에 대응 노드가 없다
(NFP·JOLTS·Sahm Rule·소매판매 등). 허브의 "대응 노드가 없는 지표" 절에 전부 나열된다.
볼트 규칙상 **아무도 부르지 않는 노드는 만들지 않으므로**, 논문·제텔이 그 개념을
실제로 쓰기 시작할 때 노드를 만든다.

## BIS credit-to-GDP gap (2026-08-14 추가)

`databook/fetchers/bis.py` · `source: bis_credit_gap`. 키 불필요.
BIS 벌크 CSV(`WS_CREDIT_GAP`)를 **프로세스당 1회만** 받아 캐시한다(국가별 지표가 여럿이어도 1회).

```yaml
  - name: BIS credit-to-GDP gap (총신용·비은행 포함)
    method: api
    source: bis_credit_gap
    countries: [US, KR, CN, JP]   # BIS 2자리 코드, 44개국 지원
    dtype: C                      # C=gap(actual-trend) · A=비율 · B=추세
    points: 4                     # 최신부터 몇 분기
```

**왜 넣었나** — 기존 신용 지표(EBP·HY OAS·신용스프레드)는 전부 신용의 **가격**이고
"지금 실물이 꺾이나"(3~12개월)만 답한다. JST(2013)의 심각도 축("확장기 신용집약도가
높을수록 침체가 깊다")을 채우려면 신용의 **양**이 필요하다. RegimeView 6차 개정이
"볼트에 지표 없음"으로 남긴 구멍이었다.

**한계 (yaml note에도 있음)** — 분기 데이터이고 BIS 갱신이 2~3분기 지연된다.
단측 HP필터(λ=400,000)라 표본 끝단이 불안정하다. 추세는 통계적 산물이지 균형 수준이 아니다.
**주간 트리거로 쓰지 말고 배경 조건으로만 쓴다.**

## FRBSF Fernald 분기 TFP (2026-08-14 추가)

`databook/fetchers/frbsf.py` · `source: frbsf_tfp`. 키 불필요.
FRBSF 공개 xlsx(`quarterly_tfp.xlsx`)의 `quarterly` 시트를 의존성 없는 자체 리더로 읽는다.

```yaml
  - name: 미국 TFP (Fernald, 가동률 조정)
    method: api
    source: frbsf_tfp
    columns: [dLP, dtfp, dtfp_util, dk]   # 기본값
    points: 4
```

| 열 | 뜻 |
|---|---|
| `dLP` | 노동생산성 |
| `dk` | 자본투입 |
| `dtfp` | TFP |
| `dutil` | 가동률 |
| `dtfp_util` | **가동률 조정 TFP** |

**왜 넣었나** — DataBook에 생산성 지표가 하나도 없었다. 그리고 **노동생산성과 TFP는 다르다** —
노동생산성은 자본심화만으로도 오른다. 실제로 2026:Q2 기준 `dLP +1.20`인데
`dtfp_util −2.19`, `dk +3.19`다. 노동생산성 상승이 기술 개선이 아니라
자본투입·가동률에서 오고 있다는 뜻이며, 이 구분이 정책 함의를 가른다.

**한계** — 분기 데이터이고 **BEA 개정으로 과거치가 소급 수정**된다. 가동률 조정은 모형 기반이다.
미국 business sector 기준이라 BLS 비농업 노동생산성과 정의가 다르다.

## BOJ 공식 API (2026-08-26 추가)

`databook/fetchers/boj_api.py` · `source: boj_api`. **키 불필요.**
BOJ가 2026-02-18 시계열통계 검색사이트에 API를 열었다([공고](https://www.boj.or.jp/statistics/outline/notice_2026/not260218a.htm) ·
[매뉴얼](https://www.stat-search.boj.or.jp/info/api_manual.pdf)). 그 전까지 일본 축은
`boj_stat`(M2)·`boj_policy_rate`(기준대출금리) **HTML 스크레이프 2개**뿐이었다.

```yaml
  - name: BOJ 정책금리 (무담보콜 익일물)
    method: api
    source: boj_api
    db: FM01                  # 매뉴얼 Ⅱ.3(2)의 DB명
    codes: ["STRDCLUCON"]     # 계열코드(DB명 접두 없이)
    labels: ["무담보콜 익일물 평균(%)"]
    freq: D                   # D W M Q CY FY — 한 지표 안에서는 동일해야 한다
    points: 6
    yoy: true                 # (선택) 레벨 → 전년비(%) 계산
    scale: 0.0001             # (선택) 억엔 → 조엔
```

**쓰고 있는 DB·계열** (전부 메타데이터 API 실호출로 확인)

| DB | 계열 | 지표 |
|---|---|---|
| FM01 | STRDCLUCON | 무담보콜 익일물 = **실제 정책금리 조작목표** |
| MD01 | MABS1AN11@ | 마네터리베이스 전년비(공표 원본) |
| MD02 | MAM1NAM2M2MO · MAM1NAM3M3MO | M2·M3 (레벨 → 전년비 계산) |
| BS01 | MABJMTA · MABJMA5B · MABJML11 | BOJ 총자산·보유국채·당좌예금 |
| MD13 | FAAPOBAL1@ | 은행 대출 증감률 |
| PR01 | PRCG20_2200000000% 외 | PPI·수출입물가 |
| PR02 | PRCS20_5200000000% | 서비스물가 SPPI |
| CO | TK99F1000601GCQ01000 외 | 단칸 업황DI·기업 물가전망 |
| BP01 | BPBP6JYACBX12 외 | 국제수지(계절조정) |
| FM09 | FX180110001 · FX180110002 | 명목·실질 실효환율 |

**함정 4개** (전부 실제로 밟았다)

1. **한 요청의 계열은 期種가 같아야 한다.** 월차와 분기를 섞으면 400.
2. **계열코드에 `%`가 들어간다**(공표 전년비 계열). URL에 직접 붙이면 깨진다 — `params`로 넘겨 `urlencode`가 인코딩하게 한다.
3. **BOJ가 전년비를 다 주지는 않는다.** PPI·수출입물가(엔베이스)·마네터리베이스는 공표 계열이 있고,
   마네스톡·계약통화베이스 수출입물가는 **레벨뿐**이다. `..2500000000%`를 있다고 가정했다가 400을 받았다.
   없는 건 `yoy: true`로 계산하고 라벨에 "— 계산"을 붙여 **공표치와 구분**한다.
4. **고빈도 접근 금지**(매뉴얼 명시). 한 지표의 여러 계열은 콤마로 묶어 1회만 요청한다.

**메타데이터 조회**(계열코드를 찾을 때):
`https://www.stat-search.boj.or.jp/api/v1/getMetadata?format=json&lang=en&db=<DB>`
⚠ 단칸(CO)은 **99MB**다. 받아서 grep할 것 — 브라우저로 열지 말 것.

## 일본 관세청 무역통계 (2026-08-26 추가)

`databook/fetchers/japan.py` · `fetch_customs` · `source: japan_customs`. **키 불필요.**

```yaml
  - name: 일본 원자재 수입
    method: api
    source: japan_customs
    section: import_commodity   # total | country | import_commodity | export_commodity
    items: ["PETROLEUM", "LNG", "COAL"]   # XML 표기와 **정확일치**
    quantity: true              # 금액만이 아니라 물량(TKL·TMT)까지
    months: 3
```

**왜 넣었나** — BOJ 국제수지는 재화수지 **총액만** 준다. 어느 나라와 무엇을 얼마나 주고받았는지는
관세청 속보에만 있고, 원유·LNG·석탄은 **수량까지** 나온다. 그래서
"수입액 급등이 가격 때문인가 물량 때문인가"가 한 화면에서 갈린다
(2026-07 실측: 원유 금액 +87.8% / 물량 +5.5% → **가격**).

**함정 3개**

1. **파일명 규칙을 추측하지 말 것.** `2026074e` · `2025_216e`처럼 불규칙하다 —
   색인 페이지 링크를 읽고 각 XML의 `<taishoymtonen>`으로 실제 대상 월을 확인한다.
2. **금액 단위가 XML에 없다**(百万円, XSL에만 표기). 2026-06 수출을 BOJ 재화수출과 대조해 확인했다
   — 관세청 10.93조엔 / BOP 10.48조엔(BOP의 FOB·소유권 조정분).
3. **음수 표기가 두 가지다**: 총액 페이지 `△634,500`, 국가별 페이지 `-634,500`.
   그리고 항목 매칭은 **정확일치**다 — 부분일치면 `EU`가 `WESTERN EUROPE`까지 끌어온다.

## e-Stat (일본 정부통계 종합창구) — 2026-08-26 추가

`databook/fetchers/e_stat.py` · `source: e_stat`. **`E_STAT_APP_ID` 필요**(무료, https://www.e-stat.go.jp/api).
※ 발급 폼의 URL 칸은 `http://localhost` · 사설 IP를 **거부**한다(공식 안내문과 다르다).
공개 저장소 주소를 넣으면 통과한다.

```yaml
  - name: 일본 기계수주 (선박·전력 제외 민수)
    method: api
    source: e_stat
    stats_data_id: "0003355222"
    series:
      - label: "민수(선박·전력 제외) 계절조정, 조엔"
        filters: {cdCat01: "160", cdCat02: "100"}
    points: 6
    scale: 0.000001          # 百万円 → 조엔
```

### ⚠ 붙이기 전에 반드시 갱신일을 볼 것

**e-Stat은 부처마다 갱신 주기가 딴판이다.** 같은 API인데 어떤 통계는 어제 것이고
어떤 통계는 5년 전에 멈췄다. `getStatsList`의 `UPDATED_DATE`를 먼저 확인한다.
2026-08-26 실측:

| 통계 | statsCode | 상태 | 조치 |
|---|---|---|---|
| 機械受注 | 00100401 | ✅ 2026-08-19 갱신 · 2026-06까지 | e-Stat 연결 |
| 家計調査 | 00200561 | ✅ 2026-08-07 갱신 · 2026Q2까지 | e-Stat 연결(분기) |
| 鉱工業指数 | 00550300 | ⚠ 2026-06-03 갱신 · **2026-03까지(≈5개월 지연)** | 연결하되 "추세용" 명시 |
| 商業動態統計 | 00550030 | ✗ **2024년치까지** | OECD(DBnomics)로 우회 |
| 毎月勤労統計 | 00450071 | ✗ **2021-10 이후 중단** | **수동 슬롯으로 확정** |

`商業動態統計`은 표 1,466개를 전부 페이징해 확인했다 — `limit`에 잘린 목록만 보고
판단하면 안 된다(552개짜리 毎月勤労統計도 마찬가지).

### 함정 3개 (전부 실제로 밟았다)

1. **`limit`으로 자른 뒤 정렬하면 안 된다.** e-Stat은 반환 순서를 보장하지 않는다.
   60건만 받아 정렬했더니 기간 100개 중 앞쪽만 잡혀 **"최신"이 2022-11**로 나왔다.
   넉넉히 받고 **정규화한 기간으로** 정렬한다.
2. **시간축에 기간이 아닌 행이 섞인다.** 鉱工業指数 표에는 `付加生産ウエイト`(가중치)가
   시간축 항목으로 들어 있어 사전순 정렬에서 맨 앞에 온다. 기간 형식 검사로 버린다.
3. **분류코드를 추측하지 말 것.** 같은 기계수주 안에서도 표 0003355268은 cat01=기종,
   0003355222는 cat01=수요자다. `getMetaInfo`로 확인한 뒤 yaml에 적는다.
   시간축 이름도 표마다 `2026年6月` / `202607` 두 가지가 있다.

## 일본 — 남은 구멍과 그 처리 (2026-08-26 확정)

자동화가 안 되는 것을 **빈칸으로 두지 않고 `manual` 슬롯으로 확정**했다.
"자동 수집이 안 된다"와 "그 지표가 필요 없다"는 다른 말이기 때문이다.

- **임금(毎月勤労統計·춘투)** → `method: manual`, **tier 1**.
  e-Stat이 2021-10에 멈췄고 통계코드도 그 하나뿐이며, 춘투 인상률은 애초에 統計가 아니라 連合 집계다.
  그런데 **BOJ 정상화의 전제가 "임금 → 서비스물가 전가"**라, 자동 수집 중인 SPPI와 대조할
  임금 계열이 없으면 그 판정 자체가 성립하지 않는다. 그래서 비우지 않고 사람이 채우는 자리로 뒀다.
  입력처는 yaml note에 URL로 박아뒀다.
- **소매 명목 판매액**: OECD 우회분은 **판매량(수량) 지수**다. 명목 금액은 商業動態統計이
  필요한데 e-Stat이 2024년치까지라 막혔다. 물가가 오르면 수량과 금액의 부호가 갈릴 수 있으니
  "소매판매가 줄었다"를 금액 기준으로 말하지 말 것.
- **광공업생산 속보치**: METI 사이트는 파일 다운로드가 **HTTP 202 + 본문 0바이트**로 막힌다
  (브라우저 UA·쿠키·리퍼러 전부 무효). **우회를 더 시도하지 말 것** — e-Stat 지연분으로
  추세만 보고, 속보는 METI 보도자료 PDF를 사람이 본다.

## 일본 파트 티어 (2026-08-26 조정)

일본 지표가 7개에서 29개로 늘었는데 **tier 1(해석 필수)이 JGB 하나뿐**이었다.
그러면 수집만 늘고 판정으로 전환되지 않는다. 그래서 BOJ 정상화 경로가 갈리는 지점 셋만 올렸다:

| 지표 | tier | 왜 |
|---|---|---|
| 일본 국채 (JGB) | 1 | 기존 — "커브가 이미 반영한 것을 먼저 뺀다" |
| 일본 CPI (코어·코어코어) | 1 ← 2 | BOJ 목표 변수. 코어와 코어코어의 **격차**가 수입물가 잔향인지 국내 압력인지를 가른다 |
| 일본 기업 물가전망 (단칸) | 1 ← 2 | 기대인플레를 **가격을 매기는 주체**에게 물은 값. 앵커링 판정의 본체 |
| 일본 임금 (수동) | 1 | 위 참조 — 전가 판정의 반쪽 |

나머지 25개는 tier 2(수치 인용만)로 둔다. **전부 tier 1로 올리면 아무것도 tier 1이 아니게 된다.**

## 지표 신뢰성 규칙 (2026-08-26 신설)

1. **미러보다 원천.** FRED·DBnomics 미러는 계열 개편·단종을 "값이 안 변함"으로만 드러낸다.
   실제로 유로존 HICP(EZ19)·GDP(EA19)·OECD CLI 3종이 그렇게 죽은 채 통과하고 있었다.
   Eurostat·ECB는 키 불필요 원천 API가 있으므로 `fetchers/europe.py`를 쓴다.
2. **유로존 집계는 EA21.** 2026-01 불가리아 가입으로 바뀌었다. EA20/EA19는 2025-12에서 끊긴다.
   Eurostat은 빈 결과에도 HTTP 200을 주므로 `fetch_eurostat`이 "시리즈 1개로 안 좁혀짐"으로 명시 실패시킨다.
3. **HICP는 ei_cphi_m만 살아 있다.** `prc_hicp_manr`·`prc_hicp_midx`·ECB `ICP`는 2026년 개편으로 전부 2025-12 정지.
4. **갱신정지는 `staleness.py`가 자동 판정.** 관측 간격을 데이터에서 추정해 `중앙간격 × 2.5 + 10일`을 넘으면
   `⛔ 갱신정지 의심`. 발표 지연이 구조적으로 긴 지표는 yaml에 `max_age_days`로 임계를 따로 준다.
   **처음부터 잘못된 코드는 못 잡는다** — 새 지표는 실호출로 최신 관측일을 확인하고 `note`에 "확인 완료(YYYY-MM-DD)"를 남긴다.
5. **뉴스 슬롯은 60일 컷.** 쿼리가 빈약하면 몇 년 전 기사가 최신인 척 지표 자리를 채운다(ISM 항목에 2023-02 기사가 앉아 있었다).
   `news_max_age_days`로 조정하고, 최근 기사가 없으면 옛 기사를 싣지 말고 실패로 표시한다.
6. **관세청 수출은 기간구분을 섞지 않는다.** 10일·20일·월전체 행이 함께 오므로 라벨에 기간구분을 넣어 분리한다.
   품목별(Itemtrade)은 `YYYY-MM` 형태가 아닌 행(연간·누계)을 버린다. 같은 계열에서 10배 이상 튀면 `error`로 경고한다.

## 중국 해관총서(GACC) 무역통계 — 2026-08-26 추가

`databook/fetchers/china.py` · `source: gacc`. **키 불필요.**

```yaml
  - name: 중국 원자재 수입
    method: api
    source: gacc
    report: imports        # totals | country | imports | exports
    currency: USD          # USD | CNY
    items: ["Crude petroleum oils", "Iron ores and concentrates"]   # GACC 표기와 정확일치
    quantity: true
    months: 3
```

### ⚠ 중국은 자동 경로가 대부분 막혀 있다 (2026-08-26 전수 실측)

| 경로 | 결과 | 조치 |
|---|---|---|
| **GACC 영문 속보** | ✅ **2026-07까지** | 연결 — 중국에서 유일하게 당월에 가까운 계열 |
| FRED `TRESEGCNM052N`(IMF IFS 외환보유액) | ✅ 2026-06 | 연결 |
| DBnomics NBS 미러 | ⚠ **2026-02까지(≈6개월)** | 연결하되 STALE 표시 + note에 경고 |
| NBS 공식 API `data.stats.gov.cn/easyquery.htm` | ✗ **403 WAF(UrlACL)** | IP 차단 — 우회 시도 금지 |
| GACC 통계포털 `stats.customs.gov.cn` | ✗ **412** | 안티봇 |
| PBoC `.../116319/`(통계데이터) 색인 | ✗ JS 렌더 | 링크가 안 잡힌다 — 2026-07 "불가" 판정의 원인 |
| **PBoC `.../116225/`(데이터해석) 색인** | ✅ **2026-07까지** | **월간 TSF 연결**(`source: pboc`) |
| DBnomics SAFE 미러 | ✗ **2020-12에서 정지** | 국제수지·대외채무는 자동 경로 없음 |

**"중국 데이터가 없다"가 아니라 "어느 경로가 언제까지인지"가 문제다.** 지표를 붙이기 전에
반드시 최신 관측월을 찍어보고, 지연분은 note에 기준월을 적는다.

### GACC 함정 4개 (전부 실제로 밟았다)

1. **월별 파일명이 GUID다.** 규칙이 없으니 색인
   (`http://english.customs.gov.cn/statics/report/preliminary.html`)의 표에서 링크를 읽어야 한다.
2. **색인의 `<a href=...>`에 따옴표가 없다**(`href=http://...`). 인용부호를 요구하는 정규식은 0건을 반환한다.
3. **표마다 단위가 다르다** — 총액표는 `USD 100 Million`(억달러), 국가별·품목별은
   `USD1 Million`(백만달러). 섞으면 100배 틀린다. fetcher가 **억달러로 통일**한다.
4. **전년비가 연초누계(1-7 Total) 기준**이다 — 당월 모멘텀이 아니다. 당월 물량·금액은 별도 열에 있다.

### 왜 물량을 같이 받나

중국은 글로벌 원자재 수요의 본체다. GACC는 **물량과 금액을 함께** 주기 때문에
"수입액이 늘었다"가 수요인지 가격인지 그 자리에서 갈린다.
2026-07 누계 실측: **원유 물량 −13.2% / 금액 +0.9%**(가격이 물량 감소를 상쇄),
구리광 **물량 −1.8% / 금액 +35.5%**(순수 가격), 철광석 물량 +5.9% / 금액 +11.6%.
금액만 보면 "중국 수요 견조"로 읽히지만 물량은 정반대다.

### 중국에서 아직 비어 있는 자리

- ~~월간 TSF~~ → **해결됨(2026-08-26)**. 아래 "PBoC 월간 금융통계" 절 참조.
- **국제수지·대외채무(SAFE)** — 위 표 참조. 분기 보도자료를 사람이 본다.
- **차이신 PMI** — S&P Global 유료. 공식 PMI(NBS)만 자동, 그나마 6개월 지연.
- **전력 생산** — NBS 미러 2025-12까지. 교차검증 층으로 쓰려면 지연을 감안할 것.

## PBoC 월간 금융통계 (TSF) — 2026-08-26 추가

`databook/fetchers/china.py` · `fetch_pboc` · `source: pboc`. **키 불필요.**

```yaml
  - name: 중국 신용 임펄스 (TSF·M1M2·위안대출)
    method: api
    source: pboc
    months: 4
```

### 왜 "불가"가 뒤집혔나 — 판정 오류의 교훈

2026-07-19에 **"PBoC는 매월 URL이 바뀌는 리포트로만 발표하므로 자동화 불가"**로 확정했었다.
실제로는 **색인 페이지를 잘못 봤던 것**이다:

| 페이지 | 성격 | 결과 |
|---|---|---|
| `/diaochatongjisi/116219/116319/` (통계데이터) | JS 렌더 | 링크 0건 → "불가" 결론 |
| `/diaochatongjisi/116219/**116225**/` (데이터해석) | **서버 렌더** | 월보 링크가 그대로 있다 |

**교훈: "그 사이트는 안 된다"가 아니라 "그 URL은 안 된다"였다.**
한 페이지가 막혔다고 기관 전체를 포기 목록에 올리지 말 것 —
같은 기관의 인접 섹션을 최소 한 번은 더 볼 것.

### 함정 3개

1. **http는 302로 튕긴다.** https만 쓴다.
2. **월 이름이 분기·반기로 바뀐다** — 3월분은 「一季度」, 6월분은 「上半年」,
   9월분은 「三季度」, 12월분은 「2025年金融统计数据报告」(월 표기 없음). 제목에서 기간을 환산한다.
3. **표가 아니라 산문에서 숫자를 뽑는다.** 특히 `人民币贷款余额`가 **두 번** 나온다 —
   앞은 TSF 하위항목(`对实体经济发放的...`), 뒤가 총잔액(`月末人民币贷款余额`)이다.
   앵커 없이 첫 매치를 쓰면 **다른 지표를 그 이름으로 싣는다**(실제로 겪었다).
   `同比增长`/`同比下降` 부호도 뒤집어야 한다.

### 읽는 법

2026-07 실측: TSF 존량 **463.27조위안(+7.4%)** · 연초누계 증량 **22.25조위안**(전년비 −1.74조) ·
M2 **+7.7%** · M1 **+4.0%** · 실체경제 위안화대출 **+5.2%** · **정부채권 잔액 +14.1%**.

**총계만 보면 구성이 안 보인다.** 지금 중국 신용은 재정(정부채권 +14.1%)이 끌고
민간 대출(+5.2%)은 처져 있다. M1이 M2에 한참 못 미치는 것도 같은 이야기다.

⚠ NBS 미러의 중국 M2(2026-02, +9.0%)와 **기준월도 값도 다르다.** 인용 시 어느 쪽인지 밝힐 것 —
**PBoC가 원출처이고 최신**이다.
